apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "djangoapp.fullname" . }}-test-connection"
  labels:
    {{- include "djangoapp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "djangoapp.fullname" . }}:{{ .Values.servicePort }}']
  restartPolicy: Never
